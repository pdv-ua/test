// /* global AC UB appAC */

exports.formCode = {
  initComponentStart
}

function initComponentStart () {
  // const me = this
  // me.on('controlChanged', controlChanged, me)
}

// function controlChanged (ctrl) {
//   const me = ctrl.up('form')
//   if (ctrl.displayTplData) {
//     const codeField = ctrl.displayTplData[0].code
//     me.fields.find(o => o.name === 'code').setValue(codeField)
//   }
// }

/* global HR UB */

exports.formDef = {
  // requiredFields: [],
  parentConfig: {
    postOnlySimpleAttributes: true,
    layout: { type: 'border' },
    defaults: {
      layout: 'fit'
    }
  },
  size: {
    width: 1100,
    height: 600
  },
  items: [
    {
      region: 'north',
      autoScroll: true,
      //   split: true,
      layout: { type: 'vbox', align: 'stretch' },
      items: [
        {
          layout: { type: 'hbox' },
          items: [
            {
              layout: { type: 'vbox' },
              defaults: {
                labelWidth: 170
              },
              flex: 1,
              items: [
                { attributeName: 'code' },
                { attributeName: 'name', width: '100%' },
                { attributeName: 'dateFromEmpty' },
                { attributeName: 'dateToEmpty' }

              ]
            }
          ]
        }
      ]
    },
    {
      xtype: 'tabpanel',
      region: 'center',
      margin: '8 8 0 8',
      items: [
        {
          title: UB.i18n('Розмір нарахування'),
          layout: { type: 'border' },
          items: [
            {
              layout: { type: 'vbox', align: 'stretch' },
              region: 'north',
              items: [
                { xtype: 'label', text: UB.i18n('Розмір нарахування залежно від KPI'), margin: '10 0 0 15', cls: 'x-form-item-label' }
              ]
            },
            {
              xtype: 'ubdetailgrid',
              region: 'center',
              name: 'rateGrid',
              autoScroll: true,
              pageSize: 10000,
              rowEditing: true,
              hideActions: ['addNewByCurrent', 'edit', 'newVersion', 'history', 'showPreview', 'itemLink', 'commandLink', 'showDetail'],
              flex: 1,
              entityConfig: {
                entity: 'hr_dictKpiAccrualRate',
                method: 'select',
                fieldList: [
                  { name: 'KPI', config: { width: 200 } },
                  {
                    name: 'rate',
                    config: { width: 200 }
                  },
                  { name: 'paySum', config: { width: 200 } }
                ]
              },
              masterFields: ['ID'],
              detailFields: ['dictKpiAccrualID']
            }
          ]
        },
        {
          title: UB.i18n('Види оплати'),
          layout: { type: 'border' },
          items: [
            {
              layout: { type: 'vbox', align: 'stretch' },
              region: 'north',
              items: [
                { xtype: 'label', text: UB.i18n('Види оплати для яких застосовуєтья нарахування за KPI'), margin: '10 0 0 15', cls: 'x-form-item-label' }
              ]
            },
            {
              xtype: 'ubdetailgrid',
              region: 'center',
              name: 'payEl',
              autoScroll: true,
              hideActions: ['addNew', 'addNewByCurrent', 'edit', 'newVersion', 'history', 'showPreview', 'itemLink', 'commandLink', 'showDetail'],
              flex: 1,
              entityConfig: {
                entity: 'hr_dictKpiAccrualPayEl',
                method: 'select',
                fieldList: [
                  {
                    name: 'payElID.code',
                    description: UB.i18n('Код'),
                    simpleFilter: true,
                    config: { width: 200 }
                  },
                  {
                    name: 'payElID.name',
                    description: UB.i18n('Назва'),
                    simpleFilter: true
                  }
                ]
              },
              masterFields: ['ID'],
              detailFields: ['dictKpiAccrualID'],
              cmpInitConfig: {
                customActions: [
                  {
                    text: UB.i18n('Редагувати'),
                    iconCls: 'u-icon-edit-alt',
                    cls: 'fill-action',
                    scale: 'medium',
                    handler: function (btn) {
                      const grid = btn.up('grid')
                      const form = grid.up('form')
                      if (form.isNewInstance) {
                        form.saveForm().then(result => {
                          if (result !== -1) {
                            form.setPayEl(form, grid)
                          }
                        })
                      } else {
                        form.setPayEl(form, grid)
                      }
                    }
                  }
                ],
                onItemDblClick: function (grid) {
                  const form = grid.up('form')
                  form.setPayEl(form, grid)
                }
              }
            }
          ]
        },
        {
          title: UB.i18n('Організації'),
          layout: { type: 'border' },
          items: [
            {
              layout: { type: 'vbox', align: 'stretch' },
              region: 'north',
              items: [
                {
                  attributeName: 'excludeOrg'
                }
              ]
            },
            {
              xtype: 'ubdetailgrid',
              region: 'center',
              name: 'organization',
              autoScroll: true,
              hideActions: ['addNew', 'addNewByCurrent', 'edit', 'newVersion', 'history', 'showPreview', 'itemLink', 'commandLink', 'showDetail'],
              flex: 1,
              entityConfig: {
                entity: 'hr_dictKpiAccrualCond',
                method: 'select',
                fieldList: [
                  {
                    name: 'orgID.description',
                    description: UB.i18n('Організація'),
                    simpleFilter: true
                  }
                ],
                whereList: {
                  orgID: {
                    expression: '[orgID.state]',
                    condition: 'equal',
                    value: 'ACTIVE'
                  },
                  conditionType: {
                    expression: '[conditionType]',
                    condition: 'equal',
                    value: '1'
                  }
                }
              },
              masterFields: ['ID'],
              detailFields: ['dictKpiAccrualID'],
              cmpInitConfig: {
                customActions: [
                  {
                    text: UB.i18n('Редагувати'),
                    iconCls: 'u-icon-edit-alt',
                    cls: 'fill-action',
                    scale: 'medium',
                    handler: function (btn) {
                      const grid = btn.up('grid')
                      const form = grid.up('form')
                      if (form.isNewInstance) {
                        form.saveForm().then(result => {
                          if (result !== -1) {
                            form.changeGridValue(form, grid)
                          }
                        })
                      } else {
                        form.changeGridValue(form, grid)
                      }
                    }
                  }
                ],
                onItemDblClick: function (grid) {
                  const form = grid.up('form')
                  form.changeGridValue(form, grid.ownerCt)
                },
                onDeterminateForm: function () {
                  return {
                    cmpInitConfig: {
                      defaultValues: { conditionType: '1' }
                    }
                  }
                }
              }
            }
          ]
        },
        {
          title: UB.i18n('Підрозділи'),
          layout: { type: 'border' },
          items: [
            {
              layout: { type: 'vbox', align: 'stretch' },
              region: 'north',
              items: [
                {
                  attributeName: 'excludeDepartment'
                }
              ]
            },
            {
              xtype: 'ubdetailgrid',
              region: 'center',
              name: 'department',
              autoScroll: true,
              hideActions: ['addNew', 'addNewByCurrent', 'edit', 'newVersion', 'history', 'showPreview', 'itemLink', 'commandLink', 'showDetail'],
              flex: 1,
              entityConfig: {
                entity: 'hr_dictKpiAccrualCond',
                method: 'select',
                fieldList: [
                  {
                    name: 'departmentID.code',
                    description: UB.i18n('Код'),
                    simpleFilter: true,
                    config: { width: 200 }
                  },
                  {
                    name: 'departmentID.name',
                    description: UB.i18n('Назва'),
                    simpleFilter: true
                  }
                ],
                whereList: {
                  orgID: {
                    expression: '[departmentID.state]',
                    condition: 'equal',
                    value: 'ACTIVE'
                  },
                  conditionType: {
                    expression: '[conditionType]',
                    condition: 'equal',
                    value: '4'
                  }
                }
              },
              masterFields: ['ID'],
              detailFields: ['dictKpiAccrualID'],
              cmpInitConfig: {
                customActions: [
                  {
                    text: UB.i18n('Редагувати'),
                    iconCls: 'u-icon-edit-alt',
                    cls: 'fill-action',
                    scale: 'medium',
                    handler: function (btn) {
                      const grid = btn.up('grid')
                      const form = grid.up('form')
                      if (form.isNewInstance) {
                        form.saveForm().then(result => {
                          if (result !== -1) {
                            form.changeGridValue(form, grid)
                          }
                        })
                      } else {
                        form.changeGridValue(form, grid)
                      }
                    }
                  }
                ],
                onItemDblClick: function (grid) {
                  const form = grid.up('form')
                  form.changeGridValue(form, grid.ownerCt)
                }
              }
            }
          ]
        },
        {
          title: UB.i18n('Посади'),
          layout: { type: 'border' },
          items: [
            {
              layout: { type: 'vbox', align: 'stretch' },
              region: 'north',
              items: [
                {
                  attributeName: 'excludePosition'
                }
              ]
            },
            {
              xtype: 'ubdetailgrid',
              region: 'center',
              name: 'position',
              autoScroll: true,
              hideActions: ['addNew', 'addNewByCurrent', 'edit', 'newVersion', 'history', 'showPreview', 'itemLink', 'commandLink', 'showDetail'],
              flex: 1,
              entityConfig: {
                entity: 'hr_dictKpiAccrualCond',
                method: 'select',
                fieldList: [
                  {
                    name: 'dictPositionID.description',
                    description: UB.i18n('Посада'),
                    simpleFilter: true
                  }
                ],
                whereList: {
                  conditionType: {
                    expression: '[conditionType]',
                    condition: 'equal',
                    values: { 'conditionType': '3' }
                  }
                }
              },
              masterFields: ['ID'],
              detailFields: ['dictKpiAccrualID'],
              cmpInitConfig: {
                customActions: [
                  {
                    text: UB.i18n('Редагувати'),
                    iconCls: 'u-icon-edit-alt',
                    cls: 'fill-action',
                    scale: 'medium',
                    handler: function (btn) {
                      const grid = btn.up('grid')
                      const form = grid.up('form')
                      if (form.isNewInstance) {
                        form.saveForm().then(result => {
                          if (result !== -1) {
                            form.changeGridValue(form, grid)
                          }
                        })
                      } else {
                        form.changeGridValue(form, grid)
                      }
                    }
                  }
                ],
                onItemDblClick: function (grid) {
                  const form = grid.up('form')
                  form.changeGridValue(form, grid.ownerCt)
                }
              }
            }
          ]
        },
        {
          title: UB.i18n('Категорії персоналу'),
          layout: { type: 'border' },
          items: [
            {
              layout: { type: 'vbox', align: 'stretch' },
              region: 'north',
              items: [
                {
                  attributeName: 'excludeStaff'
                }
              ]
            },
            {
              xtype: 'ubdetailgrid',
              region: 'center',
              name: 'dictStaffCat',
              autoScroll: true,
              hideActions: ['addNew', 'addNewByCurrent', 'edit', 'newVersion', 'history', 'showPreview', 'itemLink', 'commandLink', 'showDetail'],
              flex: 1,
              entityConfig: {
                entity: 'hr_dictKpiAccrualCond',
                method: 'select',
                fieldList: [
                  {
                    name: 'dictStaffCatID.name',
                    description: UB.i18n('Категорія персоналу')
                  }
                ],
                whereList: {
                  conditionType: {
                    expression: '[conditionType]',
                    condition: 'equal',
                    values: { 'conditionType': '2' }
                  }
                }
              },
              masterFields: ['ID'],
              detailFields: ['dictKpiAccrualID'],
              cmpInitConfig: {
                customActions: [
                  {
                    text: UB.i18n('Редагувати'),
                    iconCls: 'u-icon-edit-alt',
                    cls: 'fill-action',
                    scale: 'medium',
                    handler: function (btn) {
                      const grid = btn.up('grid')
                      const form = grid.up('form')
                      if (form.isNewInstance) {
                        form.saveForm().then(result => {
                          if (result !== -1) {
                            form.changeGridValue(form, grid)
                          }
                        })
                      } else {
                        form.changeGridValue(form, grid)
                      }
                    }
                  }
                ],
                onItemDblClick: function (grid) {
                  const form = grid.up('form')
                  form.changeGridValue(form, grid.ownerCt)
                }
              }
            }
          ]
        },
        {
          title: UB.i18n('Місце роботи'),
          layout: { type: 'border' },
          items: [
            {
              layout: { type: 'vbox', align: 'stretch' },
              region: 'north',
              items: [
                {
                  attributeName: 'excludeWorkPlace'
                }
              ]
            },
            {
              xtype: 'ubdetailgrid',
              region: 'center',
              name: 'workPlace',
              autoScroll: true,
              hideActions: ['addNew', 'addNewByCurrent', 'edit', 'newVersion', 'history', 'showPreview', 'itemLink', 'commandLink', 'showDetail'],
              flex: 1,
              entityConfig: {
                entity: 'hr_dictKpiAccrualCond',
                method: 'select',
                fieldList: [
                  {
                    name: 'workPlace',
                    description: UB.i18n('Місце роботи')
                  }
                ],
                whereList: {
                  conditionType: {
                    expression: '[conditionType]',
                    condition: 'equal',
                    values: { 'conditionType': '5' }
                  }
                }
              },
              masterFields: ['ID'],
              detailFields: ['dictKpiAccrualID'],
              customActions: [
                {
                  text: UB.i18n('Редагувати'),
                  iconCls: 'u-icon-edit-alt',
                  cls: 'fill-action',
                  scale: 'medium',
                  handler: function (btn) {
                    const grid = btn.up('grid')
                    const form = grid.up('form')
                    if (form.isNewInstance) {
                      form.saveForm().then(result => {
                        if (result !== -1) {
                          form.changeGridValue(form, grid)
                        }
                      })
                    } else {
                      form.changeGridValue(form, grid)
                    }
                  }
                }
              ],
              onItemDblClick: function (grid) {
                const form = grid.up('form')
                form.changeGridValue(form, grid.ownerCt)
              }
            }
          ]
        },
        {
          title: UB.i18n('Форма зайнятості'),
          layout: { type: 'border' },
          items: [
            {
              layout: { type: 'vbox', align: 'stretch' },
              region: 'north',
              items: [
                {
                  attributeName: 'excludeWorkerType'
                }
              ]
            },
            {
              xtype: 'ubdetailgrid',
              region: 'center',
              name: 'workerType',
              autoScroll: true,
              hideActions: ['addNew', 'addNewByCurrent', 'edit', 'newVersion', 'history', 'showPreview', 'itemLink', 'commandLink', 'showDetail'],
              flex: 1,
              entityConfig: {
                entity: 'hr_dictKpiAccrualCond',
                method: 'select',
                fieldList: [
                  {
                    name: 'workerType',
                    description: UB.i18n('Форма зайнятості')
                  }
                ],
                whereList: {
                  conditionType: {
                    expression: '[conditionType]',
                    condition: 'equal',
                    values: { 'conditionType': '6' }
                  }
                }
              },
              masterFields: ['ID'],
              detailFields: ['dictKpiAccrualID'],
              customActions: [
                {
                  text: UB.i18n('Редагувати'),
                  iconCls: 'u-icon-edit-alt',
                  cls: 'fill-action',
                  scale: 'medium',
                  handler: function (btn) {
                    const grid = btn.up('grid')
                    const form = grid.up('form')
                    if (form.isNewInstance) {
                      form.saveForm().then(result => {
                        if (result !== -1) {
                          form.changeGridValue(form, grid)
                        }
                      })
                    } else {
                      form.changeGridValue(form, grid)
                    }
                  }
                }
              ],
              onItemDblClick: function (grid) {
                const form = grid.up('form')
                form.changeGridValue(form, grid.ownerCt)
              }
            }
          ]
        }
      ]
    }

  ]
}

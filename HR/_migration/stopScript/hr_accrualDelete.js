module.exports.run = (conn) => {
  /* conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM tim_timeSheet WHERE employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null)'
  })
  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM hr_calcQueue WHERE  employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null)'
  })
  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM hr_calcTimeSheetQueue WHERE  employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null)'
  })
  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM hr_employeeNumState WHERE  employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null)'
  })
  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM hr_taxIndividAcc WHERE accrualID in (SELECT ID FROM hr_accrual WHERE employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null))'
  })

  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM hr_accrualAvg WHERE accrualID in (SELECT ID FROM hr_accrual WHERE employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null))'
  })

  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM hr_accrualDt WHERE accrualID in (SELECT ID FROM hr_accrual WHERE employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null))'
  })
  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM hr_accrual WHERE employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null)'
  })
  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM hr_accrualFundDt WHERE accrualFundID in (SELECT ID FROM hr_accrualFund WHERE employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null))'
  })
  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM hr_accrualFund WHERE  employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null)'
  })
  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: 'DELETE FROM hr_accrualBalance WHERE  employeeNumberID in (select n.ID from hr_employeeNumber n where mi_deleteUser is not null)'
  }) */
}

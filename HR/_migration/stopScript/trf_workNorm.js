module.exports.run = (conn) => {
  /*conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: `update trf_workNorm set scheduleType = '5' where scheduleType is null`
  })
  conn.xhr({
    endpoint: 'runSQL',
    URLParams: { CONNECTION: 'main' },
    data: `update trf_workNorm set code = concat(scheduleType, '-', cast(weekHours as float))`
  })*/
}
